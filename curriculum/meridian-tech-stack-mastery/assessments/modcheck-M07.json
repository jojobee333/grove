{
  "id": "modcheck-M07",
  "module": "M07",
  "title": "Module Check: AI Memory Architecture",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "Distinguish parametric memory from contextual memory in Meridian's AI layer.",
      "answer": "Parametric memory is knowledge baked into the model's weights during training — the LLM's general world knowledge. Contextual memory is dynamic: it is retrieved at inference time from external storage (pgvector), assembled into the prompt context window, and changes without retraining. Meridian uses contextual memory for user-specific facts."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "Describe the RAG write path: what happens when Meridian stores a new memory fragment?",
      "answer": "(1) Text is chunked at semantic boundaries. (2) Each chunk is embedded via the embedding model (Voyage/OpenAI). (3) The vector and metadata are inserted into the memories table with user_id + workspace_id. (4) An audit_log entry is written. On retrieval, the query is embedded and nearest neighbours are fetched with both filters."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "Why must the context window budget be managed explicitly before sending a prompt to the LLM?",
      "answer": "LLMs have a fixed token limit. Exceeding it truncates the prompt or raises an API error. Budget management ensures the system prompt, retrieved memories, conversation history, and user message all fit. The typical order of priority is: system prompt > recent messages > retrieved memories > older history."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "Which two database fields must BOTH be present in every vector search query in Meridian?",
      "options": {
        "A": "user_id and document_id",
        "B": "user_id and workspace_id",
        "C": "workspace_id and embedding_model",
        "D": "session_id and workspace_id"
      },
      "correct": "B",
      "explanation": "user_id scopes results to the right user's memories. workspace_id enforces tenant isolation. Without both, a query could return another user's memories (missing user_id) or another tenant's data (missing workspace_id). This is a non-negotiable security constraint in Meridian."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "What is the primary purpose of the LLM router in Meridian's AI layer?",
      "options": {
        "A": "Load-balance requests across multiple LLM servers",
        "B": "Direct requests to different models based on task type and cost constraints",
        "C": "Cache repeated LLM responses to reduce API calls",
        "D": "Validate that LLM outputs are safe before returning them to users"
      },
      "correct": "B",
      "explanation": "The router selects the best model for a task: cheap/fast models for autocomplete, powerful models for synthesis, specialised models for code. This optimises cost and quality. Load balancing, caching, and safety filtering are separate concerns handled by other components."
    }
  ]
}