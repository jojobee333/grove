{
  "id": "modcheck-M08",
  "module": "M08",
  "title": "Module Check: Security Architecture",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "Name the four security layers in Meridian and the primary threat each addresses.",
      "answer": "(1) Auth (JWT + bcrypt): identity forgery. (2) Tenant middleware + RLS: cross-tenant data leakage. (3) Zod validation: malformed input / injection. (4) Rate limiting + audit log: abuse, brute force, and forensic accountability."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "Why is `SET LOCAL` used instead of `SET` when setting the workspace context in PostgreSQL?",
      "answer": "`SET LOCAL` scopes the configuration variable to the current transaction. Once the transaction ends (commit or rollback), the variable resets to its prior value. `SET` persists for the entire connection, which would leak workspace context across requests in a connection pool."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "Why must JWTs have a short expiry in Meridian, and what mechanism handles re-authentication without forcing a login?",
      "answer": "Short expiry (15 min) limits the window an attacker has if a token is stolen — it becomes invalid quickly. Refresh tokens (long-lived, stored in HttpOnly cookie, single-use with rotation) handle silent re-authentication: the client exchanges the refresh token for a new access token without user interaction."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "What bcrypt cost factor does Meridian use for password hashing?",
      "options": {
        "A": "8",
        "B": "10",
        "C": "12",
        "D": "14"
      },
      "correct": "C",
      "explanation": "Meridian mandates cost factor 12. Cost 10 is the Express/bcrypt default but was calibrated for 2012 hardware. Cost 12 adds ~4x compute per hash on modern CPUs, making offline brute force significantly harder while keeping login latency acceptable (~200–400ms). 14 is overly slow for interactive auth."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "What is the defence against prompt injection attacks in Meridian's AI layer?",
      "options": {
        "A": "Reject any user message that contains the word \"ignore\"",
        "B": "Run user content through a separate moderation API before embedding it in the prompt",
        "C": "Clearly delimit system prompt from user content and never interpolate raw user input into instruction sections",
        "D": "Only allow users to select from pre-approved prompt templates"
      },
      "correct": "C",
      "explanation": "The primary defence is structural separation: system instructions and user content must be in distinct prompt sections, and user text must never be interpolated into the instruction/system section. Keyword filtering (A) is bypassable. Moderation APIs (B) help but are not the primary defence. Restricting to templates (D) removes the product's value."
    }
  ]
}