{
  "id": "modcheck-M02",
  "module": "M02",
  "title": "Module Check: State Architecture — Zustand and Yjs",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "Why must a Zustand selector return a stable reference, and what is the consequence if it does not?",
      "answer": "Zustand re-renders a component whenever the selector's return value changes by reference. Returning a new object or array literal every call (even with the same contents) causes infinite re-render loops. Selectors should return primitives or memoised stable object references."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "Explain CRDT commutativity and idempotency. Why do both properties matter for collaborative editing?",
      "answer": "Commutativity: A ⊕ B = B ⊕ A — operations can arrive in any order and produce the same result. Idempotency: A ⊕ A = A — applying the same operation twice is safe. Together they allow Yjs to merge document fragments from different peers without needing a central coordinator or explicit conflict resolution."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "What are tombstones in a Yjs document and why can't they be garbage collected immediately?",
      "answer": "Tombstones are markers for deleted content. They must remain until every peer has acknowledged the deletion, because a peer that was offline might have concurrent operations referencing the deleted position. Early GC would corrupt their operation replay."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "Which Yjs provider is used for real-time WebSocket sync in Meridian?",
      "options": {
        "A": "y-webrtc",
        "B": "y-indexeddb",
        "C": "y-websocket",
        "D": "y-dat"
      },
      "correct": "C",
      "explanation": "y-websocket connects the Yjs document to a WebSocket server — appropriate for a server-mediated collaborative app like Meridian. y-webrtc is peer-to-peer (no server relay). y-indexeddb is for offline persistence. y-dat uses the Dat protocol."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "In Zustand, what is the correct way to update a nested object property in a slice?",
      "options": {
        "A": "Mutate the property directly: `state.user.name = newName`",
        "B": "Use `set(state => ({ user: { ...state.user, name: newName } }))`",
        "C": "Call `store.setState({ user: { name: newName } })`",
        "D": "Use `produce` from immer without enabling the immer middleware"
      },
      "correct": "B",
      "explanation": "Zustand requires returning a new state object (or using immer middleware). Spreading the nested object `{ ...state.user, name: newName }` creates a new reference without mutating the original. Direct mutation breaks React's change detection. Shallow setState without spreading would lose other user properties."
    }
  ]
}