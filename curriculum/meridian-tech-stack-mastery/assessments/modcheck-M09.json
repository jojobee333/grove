{
  "id": "modcheck-M09",
  "module": "M09",
  "title": "Module Check: Monorepo and DevOps Patterns",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "What do pnpm workspaces, Turborepo, and Docker Compose each scope in the Meridian monorepo?",
      "answer": "pnpm workspaces: Node.js dependency installation and script execution across packages (client, server, db). Turborepo: task orchestration and caching — knows which packages changed and runs only affected tasks. Docker Compose: service definitions for local infrastructure (Postgres, Redis, the app containers) with network and volume management."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "What is the signal that Turborepo's caching is providing value, and what is the signal that it is not configured correctly?",
      "answer": "Value: `cache HIT` on unchanged packages — the task output is restored from cache in milliseconds. Misconfigured: `cache MISS` on every run, or stale outputs being served when source files changed. The `inputs` and `outputs` configuration in turbo.json must accurately describe what each task reads and writes."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "Why must the node_modules directory be excluded from the Docker volume mount in development?",
      "answer": "The Docker container installs its own node_modules in the image build step (for the Linux architecture). Mounting the host's node_modules on top would overwrite them with macOS/Windows binaries, causing missing native module errors inside the container."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "Why is a Docker health check necessary for the database container in Meridian's Compose setup?",
      "options": {
        "A": "To expose the database port to the host machine",
        "B": "To restart the container if Postgres crashes",
        "C": "To ensure dependent services wait until Postgres is accepting connections, not just started",
        "D": "To enable pgvector extension installation"
      },
      "correct": "C",
      "explanation": "Docker `depends_on` with `condition: service_healthy` waits until the health check passes — meaning Postgres is fully ready to accept connections. Without it, the app container starts while Postgres is still initialising and the first DB connection attempt fails."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "In a pnpm monorepo, what does the `workspace:*` version specifier in a package.json dependency mean?",
      "options": {
        "A": "Use the latest published NPM version of the package",
        "B": "Use any version of the package that satisfies peer constraints",
        "C": "Link to the package from the local workspace, using its current source",
        "D": "Pin to the exact version currently in pnpm-lock.yaml"
      },
      "correct": "C",
      "explanation": "`workspace:*` tells pnpm to resolve the dependency from a sibling package in the same monorepo — the local source, not a published registry version. This ensures changes to shared packages are immediately reflected without publishing. On `pnpm publish`, the specifier is replaced with the actual version number."
    }
  ]
}