{
  "id": "modcheck-M06",
  "module": "M06",
  "title": "Module Check: Real-Time Collaboration with Yjs",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "Explain the three CRDT properties Yjs relies on: commutativity, associativity, and idempotency.",
      "answer": "Commutativity: A⊕B = B⊕A — operations can arrive in any order. Associativity: (A⊕B)⊕C = A⊕(B⊕C) — grouping of operations doesn't matter. Idempotency: A⊕A = A — applying a duplicate operation is safe. Together they allow merge without a central coordinator."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "Name the three Yjs provider types and the use case for each.",
      "answer": "y-websocket: server-relayed real-time sync (production collaborative app). y-webrtc: peer-to-peer sync without a server (demos, LAN). y-indexeddb: offline persistence in the browser (persist state between page reloads). Meridian uses y-websocket for live collaboration and y-indexeddb for offline draft saving."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "What causes the `binding.mux infinite loop` bug in Lexical+Yjs integration and how do you prevent it?",
      "answer": "The bug occurs when a Lexical change triggers a Yjs update, which triggers a Lexical change, creating an infinite loop. The fix is the mux (mutual exclusion) guard: set a flag before applying a Yjs-originated change to Lexical, check the flag in the Lexical onChange handler, and skip re-syncing to Yjs when the flag is set."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "What does the Yjs awareness protocol track?",
      "options": {
        "A": "Document version vectors for conflict resolution",
        "B": "Per-user ephemeral state such as cursor position and selection",
        "C": "Connection health and latency between peers",
        "D": "Access control lists for document permissions"
      },
      "correct": "B",
      "explanation": "Awareness is a separate lightweight channel for ephemeral (non-document) state — cursor positions, user names, colours. It uses eventual consistency with TTL, not CRDT merging. Version vectors are part of the document sync protocol; ACLs are application-level."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "Why does Yjs prefer a client-server-client topology over pure peer-to-peer for production use?",
      "options": {
        "A": "P2P cannot handle binary data",
        "B": "The server can enforce auth, persist history, and relay to clients behind NAT",
        "C": "CRDTs only work when a central node assigns timestamps",
        "D": "WebRTC is not supported in all browsers"
      },
      "correct": "B",
      "explanation": "A server relay solves auth (unauthenticated peers cannot connect), persistence (server stores the document), and NAT traversal (peers behind firewalls can still sync). CRDTs are peer-agnostic and need no central clock. WebRTC is widely supported but lacks server-side auth hooks."
    }
  ]
}