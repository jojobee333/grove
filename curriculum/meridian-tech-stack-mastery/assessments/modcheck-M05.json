{
  "id": "modcheck-M05",
  "module": "M05",
  "title": "Module Check: React 18 Patterns and Lexical Editor",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "What is the difference between `ReactDOM.render` and `createRoot`, and why does Meridian use `createRoot`?",
      "answer": "`ReactDOM.render` is the legacy synchronous renderer (React 17 and below). `createRoot` enables React 18 concurrent features: automatic batching, transitions, and Suspense streaming. Meridian needs `startTransition` for the AI panel updates and `Suspense` for lazy-loaded editor modules."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "What does `startTransition` tell React, and when should Meridian use it?",
      "answer": "`startTransition` marks a state update as non-urgent — React may defer it to keep the UI responsive. Use it for AI result rendering and collaborative cursor updates, where dropping frames is preferable to blocking the editor's keystroke response."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "What is a Lexical EditorState and why is it immutable?",
      "answer": "EditorState is a serialisable snapshot of the entire document — nodes, selection, and metadata. It is immutable so that Lexical can maintain a history stack for undo/redo, diff states efficiently, and replay operations without side effects. Mutations go through a registered command that produces a new EditorState."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "What is React 18 automatic batching?",
      "options": {
        "A": "React bundles multiple component renders into a single DOM commit",
        "B": "Multiple `setState` calls in async callbacks are batched into a single re-render",
        "C": "Components are rendered in parallel using Web Workers",
        "D": "React skips re-renders when props are deeply equal"
      },
      "correct": "B",
      "explanation": "In React 17, setState calls inside async callbacks (setTimeout, Promise) each triggered a separate re-render. React 18 batches them automatically — all setState calls in the same event loop tick (including async) produce one re-render. Option A describes Reconciler behaviour, not automatic batching specifically."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "In Lexical, how do you safely read the document content outside of a command handler?",
      "options": {
        "A": "Access `editor._editorState.root` directly",
        "B": "Call `editor.getEditorState().read(callback)`",
        "C": "Register a mutation listener with `editor.registerMutationListener`",
        "D": "Serialise via `editor.getRootElement().innerHTML`"
      },
      "correct": "B",
      "explanation": "`editor.getEditorState().read(callback)` provides safe read-only access inside Lexical's reconciler context. Accessing internal `_editorState` breaks encapsulation. MutationListeners are for observing DOM changes, not reading document content. innerHTML bypasses Lexical's type system entirely."
    }
  ]
}