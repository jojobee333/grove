{
  "topic": "Rust for Programmers",
  "slug": "rust-for-programmers",
  "generated": "2026-04-19",
  "learner_goal": "learn",
  "total_cards": 81,
  "cards": [
    {
      "id": "CARD-001",
      "type": "definition",
      "module": "M01",
      "lesson": "L01",
      "front": "What is Cargo, and what does it do?",
      "back": "Cargo is Rust's official build system and package manager. It handles: compiling your code (cargo build), running it (cargo run), running tests (cargo test), managing dependencies in Cargo.toml, and publishing packages to crates.io.",
      "source_ref": "S006",
      "confidence_required": "low",
      "tags": ["module-M01", "claim-C1", "type-definition", "cargo"]
    },
    {
      "id": "CARD-002",
      "type": "cloze",
      "module": "M01",
      "lesson": "L02",
      "front": "In Rust, variables are {{immutable}} by default. To allow mutation, you add the {{mut}} keyword.",
      "back": "Variables are immutable by default. Use `let mut x = 5;` to make x mutable. This forces intentional mutation and makes code easier to reason about.",
      "source_ref": "S001",
      "confidence_required": "low",
      "tags": ["module-M01", "claim-C1", "type-cloze", "variables"]
    },
    {
      "id": "CARD-003",
      "type": "distinction",
      "module": "M01",
      "lesson": "L02",
      "front": "What is the difference between shadowing and mut in Rust?",
      "back": "Shadowing (`let x = x + 1;`) creates a new binding and can change the type. mut allows reassigning the same binding but not changing the type. Shadowing is preferred when transforming a value into a different type.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M01", "claim-C1", "type-distinction", "shadowing"]
    },
    {
      "id": "CARD-004",
      "type": "cloze",
      "module": "M01",
      "lesson": "L03",
      "front": "In Rust, if/else is an {{expression}}, not a statement — the last expression in each arm is the {{return value}}.",
      "back": "Example: `let result = if condition { 5 } else { 6 };` — both arms must have the same type.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M01", "claim-C1", "type-cloze", "expressions"]
    },
    {
      "id": "CARD-005",
      "type": "definition",
      "module": "M02",
      "lesson": "L04",
      "front": "What are Rust's three ownership rules?",
      "back": "1. Each value has exactly one owner. 2. There can only be one owner at a time. 3. When the owner goes out of scope, the value is dropped (RAII). These three rules together eliminate memory leaks, use-after-free, and double-free bugs.",
      "source_ref": "S001",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C2", "type-definition", "ownership"]
    },
    {
      "id": "CARD-006",
      "type": "cloze",
      "module": "M02",
      "lesson": "L04",
      "front": "When you assign `s2 = s1` where s1 is a String, Rust {{moves}} the ownership. After the move, using s1 is a {{compile error}}.",
      "back": "Move semantics mean only s2 owns the String data. If you need both, use `s2 = s1.clone()`. Types that implement Copy (like i32) are copied instead of moved.",
      "source_ref": "S001",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C2", "type-cloze", "move-semantics"]
    },
    {
      "id": "CARD-007",
      "type": "application",
      "module": "M02",
      "lesson": "L04",
      "front": "Why does Rust use move semantics instead of automatic deep copy?",
      "back": "Deep copy is expensive for heap data. By making moves the default, Rust forces programmers to explicitly choose copying with .clone() — making performance costs visible. Types that are cheap to copy (i32, bool, f64) implement Copy and are copied automatically.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-application", "move-semantics"]
    },
    {
      "id": "CARD-008",
      "type": "definition",
      "module": "M02",
      "lesson": "L05",
      "front": "What are Rust's two borrowing rules?",
      "back": "1. At any time, you may have either one mutable reference (&mut T) OR any number of immutable references (&T) — never both. 2. References must always be valid (no dangling references). These rules are enforced at compile time by the borrow checker.",
      "source_ref": "S002",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C2", "type-definition", "borrowing"]
    },
    {
      "id": "CARD-009",
      "type": "cloze",
      "module": "M02",
      "lesson": "L05",
      "front": "Rust prevents {{data races}} at compile time. A data race requires two concurrent accesses where at least one is a {{write}} and there is no synchronization.",
      "back": "Because Rust forbids simultaneous &mut T and &T references, a data race is structurally impossible in safe Rust — the borrow checker rejects the code that would allow it.",
      "source_ref": "S002",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C4", "type-cloze", "data-race"]
    },
    {
      "id": "CARD-010",
      "type": "distinction",
      "module": "M02",
      "lesson": "L06",
      "front": "What is the difference between String and &str in Rust?",
      "back": "String: heap-allocated, owned, growable sequence of UTF-8 bytes. &str: an immutable borrowed reference to a sequence of UTF-8 bytes (a 'slice'). String literals like \"hello\" have type &str and are stored in the binary. Use &str in function parameters when you don't need ownership.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-distinction", "string-str"]
    },
    {
      "id": "CARD-011",
      "type": "cloze",
      "module": "M02",
      "lesson": "L06",
      "front": "Prefer {{&str}} over {{&String}} in function parameters because of Rust's deref coercion — &String automatically coerces to &str.",
      "back": "fn greet(name: &str) accepts both &str and &String callers. fn greet(name: &String) only accepts &String. The &str version is more flexible and idiomatic.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-cloze", "deref-coercion"]
    },
    {
      "id": "CARD-012",
      "type": "definition",
      "module": "M03",
      "lesson": "L07",
      "front": "What is struct update syntax in Rust?",
      "back": "Struct update syntax `Point { x: 1.0, ..other }` creates a new struct with some fields from another instance. The `..` copies remaining fields from `other`. Note: if any moved field is not Copy, the original is partially moved.",
      "source_ref": "S008",
      "confidence_required": "low",
      "tags": ["module-M03", "claim-C1", "type-definition", "struct"]
    },
    {
      "id": "CARD-013",
      "type": "cloze",
      "module": "M03",
      "lesson": "L08",
      "front": "In a Rust impl block, methods take {{&self}}, {{&mut self}}, or {{self}} as the first parameter, corresponding to borrow, mutable borrow, or consume.",
      "back": "The method call `rect.area()` is sugar for `Rectangle::area(&rect)` — Rust automatically references/dereferences to match the method signature.",
      "source_ref": "S008",
      "confidence_required": "medium",
      "tags": ["module-M03", "claim-C1", "type-cloze", "methods"]
    },
    {
      "id": "CARD-014",
      "type": "distinction",
      "module": "M03",
      "lesson": "L08",
      "front": "What is the difference between a method and an associated function in Rust?",
      "back": "Methods take self as the first parameter and are called with dot syntax (instance.method()). Associated functions don't take self and are called with :: syntax (Type::function()). Associated functions are typically used as constructors: `Rectangle::new(width, height)`.",
      "source_ref": "S008",
      "confidence_required": "medium",
      "tags": ["module-M03", "claim-C1", "type-distinction", "associated-function"]
    },
    {
      "id": "CARD-015",
      "type": "application",
      "module": "M03",
      "lesson": "L09",
      "front": "Why should you prefer owned types (String) over references (&str) in structs, especially early in learning Rust?",
      "back": "Storing references in structs requires lifetime annotations — the compiler needs to know the borrowed data lives at least as long as the struct. Using owned types (String instead of &str) avoids this requirement entirely. Prefer owned types in structs until lifetimes are well understood.",
      "source_ref": "S008",
      "confidence_required": "medium",
      "tags": ["module-M03", "claim-C2", "type-application", "struct-ownership"]
    },
    {
      "id": "CARD-016",
      "type": "definition",
      "module": "M04",
      "lesson": "L10",
      "front": "How do Rust enums differ from enums in Java or C?",
      "back": "Rust enums can have data attached to each variant. Example: `enum Message { Quit, Move { x: i32, y: i32 }, Write(String), ChangeColor(i32, i32, i32) }`. Each variant can have different data types and amounts. This eliminates the need for class hierarchies for sum types.",
      "source_ref": "S009",
      "confidence_required": "medium",
      "tags": ["module-M04", "claim-C3", "type-definition", "enum"]
    },
    {
      "id": "CARD-017",
      "type": "cloze",
      "module": "M04",
      "lesson": "L11",
      "front": "Rust's match is {{exhaustive}} — the compiler ensures all possible variants are {{handled}}. This prevents forgotten cases at compile time, not at runtime.",
      "back": "If you add a new variant to an enum, every match expression on that enum becomes a compile error until you handle the new variant. The `_` pattern acts as a catch-all.",
      "source_ref": "S009",
      "confidence_required": "high",
      "tags": ["module-M04", "claim-C3", "type-cloze", "match"]
    },
    {
      "id": "CARD-018",
      "type": "definition",
      "module": "M04",
      "lesson": "L12",
      "front": "What is Option<T> and why does Rust use it instead of null?",
      "back": "Option<T> is an enum: `Some(T)` (contains a value) or `None` (no value). The type system forces you to handle both cases — you cannot use an Option<T> as if it were T without unwrapping it. This eliminates null pointer exceptions at compile time. Rust has no null value.",
      "source_ref": "S009",
      "confidence_required": "high",
      "tags": ["module-M04", "claim-C3", "type-definition", "option"]
    },
    {
      "id": "CARD-019",
      "type": "cloze",
      "module": "M04",
      "lesson": "L12",
      "front": "`Option::map` applies a function to the inner value if {{Some}}, and returns {{None}} if the option is None — without unwrapping.",
      "back": "Example: `Some(5).map(|x| x * 2)` = `Some(10)`. `None.map(|x: i32| x * 2)` = `None`. Chain with `and_then` for operations that also return Option.",
      "source_ref": "S009",
      "confidence_required": "medium",
      "tags": ["module-M04", "claim-C3", "type-cloze", "option-map"]
    },
    {
      "id": "CARD-020",
      "type": "application",
      "module": "M05",
      "lesson": "L13",
      "front": "Why should you use .get(index) instead of v[index] when accessing Vec elements?",
      "back": ".get(index) returns Option<&T> — Some(&value) if in bounds, None if out of bounds. v[index] panics if out of bounds. Use .get() when the index might be invalid; use [] only when you're certain the index is valid (e.g., within a loop bounded by v.len()).",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-application", "vec"]
    },
    {
      "id": "CARD-021",
      "type": "cloze",
      "module": "M05",
      "lesson": "L14",
      "front": "The HashMap entry API `map.entry(key).or_insert({{0}})` safely inserts a default value if the key is absent, and returns a {{mutable reference}} to the value either way.",
      "back": "Example for counting: `*map.entry(word).or_insert(0) += 1;` — the `*` dereferences the &mut i32 to increment it. This is more efficient than checking contains_key and then inserting.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-cloze", "hashmap-entry"]
    },
    {
      "id": "CARD-022",
      "type": "definition",
      "module": "M05",
      "lesson": "L15",
      "front": "Why can't you index into a String with `s[0]` in Rust?",
      "back": "String is a UTF-8 encoded sequence. A single char may be 1–4 bytes. Index 0 would be the first byte, not the first character — which could be the middle of a multi-byte character, producing invalid data. Rust prevents this at compile time. Use .chars().nth(0) for character access.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-definition", "utf8"]
    },
    {
      "id": "CARD-023",
      "type": "definition",
      "module": "M06",
      "lesson": "L16",
      "front": "What is the difference between panic! and Result in Rust error handling?",
      "back": "panic! is for unrecoverable errors — states that should never happen (programmer bugs, invariant violations). It unwinds the stack and terminates. Result<T, E> is for recoverable errors — expected failure modes the caller should handle (file not found, parse failure, network timeout). Use Result at API boundaries; use panic! only for bugs.",
      "source_ref": "S003",
      "confidence_required": "high",
      "tags": ["module-M06", "claim-C5", "type-definition", "result-panic"]
    },
    {
      "id": "CARD-024",
      "type": "cloze",
      "module": "M06",
      "lesson": "L17",
      "front": "The `?` operator in Rust desugars to: if Err, {{return Err(e.into())}}; if Ok, {{unwrap and continue}}.",
      "back": "The `.into()` call invokes From::from, converting the error type automatically. This means ? works across different error types as long as From implementations exist. ? can only be used in functions that return Result (or Option).",
      "source_ref": "S003",
      "confidence_required": "high",
      "tags": ["module-M06", "claim-C5", "type-cloze", "question-mark"]
    },
    {
      "id": "CARD-025",
      "type": "application",
      "module": "M06",
      "lesson": "L17",
      "front": "When should you use Box<dyn Error> as the return type instead of a specific error type?",
      "back": "Use Box<dyn Error> in main() and in binary code where you want to propagate any error type without defining a custom enum. In library code, prefer a concrete error type or a custom enum so callers can pattern-match on specific errors. Box<dyn Error> trades specificity for ergonomics.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-application", "box-dyn-error"]
    },
    {
      "id": "CARD-026",
      "type": "cloze",
      "module": "M06",
      "lesson": "L18",
      "front": "A custom error type needs to implement {{Display}} (for human-readable messages) and {{std::error::Error}} (for the error trait object infrastructure).",
      "back": "Minimal impl: `impl Display for AppError { fn fmt(...) }` + `impl std::error::Error for AppError {}`. The Error trait has no required methods — the default implementation is sufficient for most cases.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-cloze", "custom-error"]
    },
    {
      "id": "CARD-027",
      "type": "definition",
      "module": "M07",
      "lesson": "L19",
      "front": "What is monomorphization in Rust generics?",
      "back": "Monomorphization is the compile-time process of creating concrete implementations of generic code for each type it's used with. `fn largest<T>(list: &[T])` called with &[i32] and &[f64] produces two compiled functions. This makes generics zero-cost — no runtime overhead from using generics.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-definition", "monomorphization"]
    },
    {
      "id": "CARD-028",
      "type": "cloze",
      "module": "M07",
      "lesson": "L20",
      "front": "The {{orphan rule}} in Rust states: you can implement a trait on a type only if either the {{trait}} or the {{type}} is defined in your crate.",
      "back": "You CAN implement Display for your own struct. You CAN implement your own trait for String. You CANNOT implement Display (foreign trait) for String (foreign type) — this prevents conflicting implementations.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-cloze", "orphan-rule"]
    },
    {
      "id": "CARD-029",
      "type": "distinction",
      "module": "M07",
      "lesson": "L20",
      "front": "What is the difference between impl Trait parameter syntax and a where clause?",
      "back": "Both express trait bounds. `fn f(x: impl Display)` = `fn f<T: Display>(x: T)`. Use where for: multiple bounds (`T: Display + Clone`), when the signature would be unreadable inline, or when the same type parameter appears multiple times. Both are equivalent to the compiler.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-distinction", "trait-bounds"]
    },
    {
      "id": "CARD-030",
      "type": "cloze",
      "module": "M07",
      "lesson": "L21",
      "front": "Lifetime annotations don't {{change}} how long references live — they describe {{relationships}} between lifetimes so the compiler can verify them.",
      "back": "In `fn longest<'a>(x: &'a str, y: &'a str) -> &'a str`, the 'a means: the return lives at least as long as the shorter of x and y. The annotation doesn't extend anyone's lifetime.",
      "source_ref": "S010",
      "confidence_required": "high",
      "tags": ["module-M07", "claim-C5", "type-cloze", "lifetime"]
    },
    {
      "id": "CARD-031",
      "type": "definition",
      "module": "M07",
      "lesson": "L21",
      "front": "What are Rust's three lifetime elision rules?",
      "back": "1. Each reference parameter gets its own lifetime parameter. 2. If there is exactly one input lifetime, that lifetime is assigned to all output lifetimes. 3. If one of the input lifetimes is &self or &mut self, the lifetime of self is assigned to all output lifetimes. If all outputs are resolved after these rules, no explicit annotations are needed.",
      "source_ref": "S010",
      "confidence_required": "high",
      "tags": ["module-M07", "claim-C5", "type-definition", "lifetime-elision"]
    },
    {
      "id": "CARD-032",
      "type": "application",
      "module": "M07",
      "lesson": "L21",
      "front": "When is the 'static lifetime appropriate, and what is the most common misuse?",
      "back": "'static is appropriate for string literals (which live in the binary) and for data that genuinely must outlive all other references. Common misuse: adding 'static to silence lifetime errors. This almost always indicates a design problem — use owned data (String instead of &str) or restructure ownership rather than demanding 'static.",
      "source_ref": "S010",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C5", "type-application", "static-lifetime"]
    },
    {
      "id": "CARD-033",
      "type": "definition",
      "module": "M08",
      "lesson": "L22",
      "front": "What is the difference between Fn, FnMut, and FnOnce closure traits?",
      "back": "FnOnce: closure can only be called once — it takes ownership of captured values. FnMut: closure can be called multiple times and may mutate captured values. Fn: closure can be called multiple times without mutating captured values (most common). Every closure implements at least FnOnce; those that don't consume captures also implement FnMut; those that don't mutate also implement Fn.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-definition", "closure-traits"]
    },
    {
      "id": "CARD-034",
      "type": "cloze",
      "module": "M08",
      "lesson": "L23",
      "front": "Iterators in Rust are {{lazy}} — iterator adapters like map() and filter() do nothing until a {{consuming}} adapter is called.",
      "back": "Example: `v.iter().map(|x| x + 1)` creates an iterator but computes nothing. Only when `.collect()`, `.sum()`, or a for loop consumes it does any work happen.",
      "source_ref": "S011",
      "confidence_required": "high",
      "tags": ["module-M08", "claim-C6", "type-cloze", "lazy-iterator"]
    },
    {
      "id": "CARD-035",
      "type": "distinction",
      "module": "M08",
      "lesson": "L23",
      "front": "What is the difference between iter(), into_iter(), and iter_mut() on a Vec?",
      "back": "iter() yields &T — immutable references, Vec is not consumed. iter_mut() yields &mut T — mutable references, Vec is not consumed. into_iter() yields T — owned values, Vec is consumed (moved). Use iter() when you want to read elements, into_iter() when you want to transform them into something new.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-distinction", "iter-variants"]
    },
    {
      "id": "CARD-036",
      "type": "application",
      "module": "M08",
      "lesson": "L24",
      "front": "Rewrite this loop as an iterator chain: `let mut result = Vec::new(); for x in &v { if x % 2 == 0 { result.push(x * x); } }`",
      "back": "`let result: Vec<_> = v.iter().filter(|x| *x % 2 == 0).map(|x| x * x).collect();` — the iterator chain is more readable, compile-equivalent in performance, and eliminates the mutable accumulator.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-application", "iterator-chain"]
    },
    {
      "id": "CARD-037",
      "type": "cloze",
      "module": "M08",
      "lesson": "L24",
      "front": "Iterator chains in Rust are a {{zero-cost abstraction}} — they compile to the same machine code as hand-written {{for loops}}.",
      "back": "The Rust compiler (via LLVM) unrolls and inlines iterator chains. You get the readability of functional style with the performance of explicit loops. There is no runtime overhead from chaining adapters.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-cloze", "zero-cost"]
    },
    {
      "id": "CARD-038",
      "type": "definition",
      "module": "M09",
      "lesson": "L25",
      "front": "What is the Send trait in Rust, and why is it required for thread::spawn?",
      "back": "Send is a marker trait that indicates a type is safe to transfer to another thread. thread::spawn requires the closure to be Send because the closure and any captured values cross a thread boundary. Rc<T> is not Send (reference counts are not atomic); Arc<T> is Send. The compiler enforces this — you cannot accidentally share non-thread-safe types across threads.",
      "source_ref": "S005",
      "confidence_required": "high",
      "tags": ["module-M09", "claim-C7", "type-definition", "send-trait"]
    },
    {
      "id": "CARD-039",
      "type": "cloze",
      "module": "M09",
      "lesson": "L25",
      "front": "Thread closures in Rust typically use the {{move}} keyword to transfer {{ownership}} of captured variables to the spawned thread.",
      "back": "Without move, the closure captures by reference — but the thread might outlive the reference's validity. move forces ownership transfer, making the reference valid for the thread's entire lifetime.",
      "source_ref": "S005",
      "confidence_required": "high",
      "tags": ["module-M09", "claim-C7", "type-cloze", "move-thread"]
    },
    {
      "id": "CARD-040",
      "type": "distinction",
      "module": "M09",
      "lesson": "L26",
      "front": "When should you use channels vs Arc<Mutex<T>> for concurrency in Rust?",
      "back": "Channels (mpsc) for message passing: prefer when threads have clear producer/consumer roles and data ownership transfers between threads. Arc<Mutex<T>> for shared state: prefer when multiple threads need to read and write the same data without passing ownership. Channels avoid the risk of deadlock; Mutex requires careful lock ordering to avoid deadlock.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M09", "claim-C7", "type-distinction", "channels-vs-mutex"]
    },
    {
      "id": "CARD-041",
      "type": "cloze",
      "module": "M09",
      "lesson": "L27",
      "front": "`Arc<T>` is the thread-safe version of `Rc<T>`. Arc uses {{atomic}} reference counting; Rc uses {{non-atomic}} reference counting. Only Arc implements {{Send}}.",
      "back": "Use Rc<T> for single-threaded scenarios (more performant). Use Arc<T> when sharing ownership across threads. Combine Arc with Mutex for mutable shared state: Arc<Mutex<T>>.",
      "source_ref": "S005",
      "confidence_required": "high",
      "tags": ["module-M09", "claim-C7", "type-cloze", "arc-rc"]
    },
    {
      "id": "CARD-042",
      "type": "application",
      "module": "M09",
      "lesson": "L27",
      "front": "Why is Rust's concurrency called 'fearless concurrency'?",
      "back": "The same ownership and type system that prevents memory bugs also prevents data races. Rc<T> isn't Send (compile error if you try to share across threads). &mut T can't be shared (borrow checker prevents simultaneous writes). Arc<Mutex<T>> enforces exclusive access at the type level. Data races are compile-time errors, not runtime races you need a profiler to find.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M09", "claim-C7", "type-application", "fearless-concurrency"]
    },
    {
      "id": "CARD-043",
      "type": "claim",
      "module": "M02",
      "lesson": "L04",
      "front": "What does the evidence say about whether ownership is just a 'feature' in Rust?",
      "back": "Ownership is not a feature — it is the organizing principle of the entire language. Lifetimes, traits, Option<T>, iterators, and concurrency all derive their safety guarantees from ownership rules. Confidence: high. Sources: S001, S002, S005, S010.",
      "source_ref": "S001",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C2", "type-claim", "ownership-central"]
    },
    {
      "id": "CARD-044",
      "type": "claim",
      "module": "M04",
      "lesson": "L12",
      "front": "What does the evidence say about teaching enums, Option<T>, and match as separate topics?",
      "back": "Teaching them separately is pedagogically inferior. Option<T> loses meaning without match; match loses motivation without data-carrying enums. The 'no null in Rust' insight is the anchor — teach all three together in one module. Confidence: high. Source: S009.",
      "source_ref": "S009",
      "confidence_required": "medium",
      "tags": ["module-M04", "claim-C3", "type-claim", "enum-match-option"]
    },
    {
      "id": "CARD-045",
      "type": "claim",
      "module": "M07",
      "lesson": "L21",
      "front": "What does the evidence say about how often explicit lifetime annotations are needed in practice?",
      "back": "Lifetime elision rules cover 80–90% of real Rust code. Most method signatures never need explicit annotations (elision rule 3 handles &self methods). Explicit 'a annotations appear mainly in library code with multi-reference functions. Confidence: medium-high. Source: S010.",
      "source_ref": "S010",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C5", "type-claim", "lifetime-elision-practice"]
    },
    {
      "id": "CARD-046",
      "type": "cloze",
      "module": "M01",
      "lesson": "L01",
      "front": "In Rust, `cargo {{new}} project_name` creates a new project; `cargo {{run}}` compiles and executes it.",
      "back": "cargo new creates Cargo.toml (manifest) and src/main.rs. cargo run is equivalent to cargo build && ./target/debug/project_name but more ergonomic.",
      "source_ref": "S006",
      "confidence_required": "low",
      "tags": ["module-M01", "claim-C1", "type-cloze", "cargo-commands"]
    },
    {
      "id": "CARD-047",
      "type": "definition",
      "module": "M06",
      "lesson": "L16",
      "front": "What does Result<T, E> represent in Rust?",
      "back": "Result<T, E> is an enum with two variants: Ok(T) for success (carrying the value), Err(E) for failure (carrying the error). It is how Rust encodes fallible operations at the type level — the caller is forced to handle both cases. Common in I/O, parsing, and any operation that can fail for external reasons.",
      "source_ref": "S003",
      "confidence_required": "high",
      "tags": ["module-M06", "claim-C5", "type-definition", "result"]
    },
    {
      "id": "CARD-048",
      "type": "cloze",
      "module": "M04",
      "lesson": "L11",
      "front": "The `if let Some(x) = option` syntax is syntactic sugar for a {{match}} with one arm; it's preferred when you only care about the {{Some}} case.",
      "back": "Equivalent: `match option { Some(x) => { /* use x */ }, _ => {} }`. Use if let when the None case needs no action; use match when you need to handle None explicitly.",
      "source_ref": "S009",
      "confidence_required": "medium",
      "tags": ["module-M04", "claim-C3", "type-cloze", "if-let"]
    },
    {
      "id": "CARD-049",
      "type": "application",
      "module": "M08",
      "lesson": "L22",
      "front": "Why do thread closures often need `move` but regular closures usually don't?",
      "back": "A spawned thread may outlive the scope where the closure was created. If the closure captured by reference, the reference could become invalid (dangling) after the original scope ends. `move` transfers ownership of captured values into the closure, ensuring they live as long as the thread. Regular closures (passed to map, filter) are called immediately in the same scope, so references remain valid.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-application", "move-closure"]
    },
    {
      "id": "CARD-050",
      "type": "cloze",
      "module": "M02",
      "lesson": "L05",
      "front": "NLL (Non-Lexical Lifetimes) means a borrow ends at its {{last use}}, not at the end of the {{enclosing block}}.",
      "back": "Before NLL, this would fail: `let mut s = String::from(\"hi\"); let r = &s; println!(\"{r}\"); s.push_str(\" there\");` — NLL sees that r is last used before the push_str, so the borrow is already released. NLL makes many valid programs compile that older Rust would reject.",
      "source_ref": "S002",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-cloze", "nll"]
    },
    {
      "id": "CARD-051",
      "type": "definition",
      "module": "M09",
      "lesson": "L26",
      "front": "What does mpsc stand for and how does the Rust channel work?",
      "back": "mpsc = Multiple Producer, Single Consumer. `std::sync::mpsc::channel()` returns `(Sender<T>, Receiver<T>)`. Multiple Sender instances (cloned with .clone()) can send values to one Receiver. Sender::send() transfers ownership of the value. Receiver::recv() blocks until a value arrives or all Senders are dropped.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M09", "claim-C7", "type-definition", "mpsc"]
    },
    {
      "id": "CARD-052",
      "type": "cloze",
      "module": "M07",
      "lesson": "L20",
      "front": "A {{blanket implementation}} implements a trait for any type satisfying a bound. Example: `impl<T: Display> Printable for T` implements Printable for {{every type that implements Display}}.",
      "back": "The Rust stdlib uses this: `impl<T: Display> ToString for T {}` — that's why any type implementing Display automatically gets a .to_string() method.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-cloze", "blanket-impl"]
    },
    {
      "id": "CARD-053",
      "type": "application",
      "module": "M05",
      "lesson": "L13",
      "front": "What happens to a Vec's elements when you call into_iter() vs iter()?",
      "back": "into_iter() consumes the Vec — each element is moved out (owned T). After into_iter(), you cannot use the Vec again. iter() borrows the Vec — each element is an immutable reference (&T). The Vec is still valid after iteration. Use iter() to inspect; use into_iter() to transform into a new collection.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-application", "into-iter"]
    },
    {
      "id": "CARD-054",
      "type": "cloze",
      "module": "M03",
      "lesson": "L09",
      "front": "The `#[derive(Debug)]` attribute automatically implements the {{Debug}} trait, enabling `{:?}` and `{:#?}` ({{pretty-print}}) format specifiers.",
      "back": "You can only derive Debug if all fields also implement Debug. For custom formatting, implement fmt::Display instead (which enables `{}`). They serve different audiences: Debug for developers, Display for end users.",
      "source_ref": "S008",
      "confidence_required": "low",
      "tags": ["module-M03", "claim-C1", "type-cloze", "debug-derive"]
    },
    {
      "id": "CARD-055",
      "type": "distinction",
      "module": "M06",
      "lesson": "L16",
      "front": "What is the difference between unwrap() and expect() on a Result?",
      "back": "Both panic on Err. unwrap() panics with a generic message: 'called Result::unwrap() on an Err value: ...' — hard to debug. expect(\"message\") panics with your custom message — much more useful in a stack trace. Prefer expect() over unwrap() whenever you must use either; prefer ? over both in production code.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-distinction", "unwrap-expect"]
    },
    {
      "id": "CARD-056",
      "type": "cloze",
      "module": "M09",
      "lesson": "L27",
      "front": "`Mutex::lock()` returns a `MutexGuard<T>`. The lock is {{automatically released}} when the guard {{goes out of scope}} (RAII).",
      "back": "You do not need to explicitly unlock. This prevents lock leaks. Use `drop(guard)` to release early if needed. Deadlock occurs if a thread calls lock() while already holding the lock — Rust does not prevent this at compile time.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M09", "claim-C7", "type-cloze", "mutex-guard"]
    },
    {
      "id": "CARD-057",
      "type": "application",
      "module": "M02",
      "lesson": "L04",
      "front": "What does RAII mean in Rust, and why is it important?",
      "back": "RAII (Resource Acquisition Is Initialization): resources (heap memory, file handles, locks) are tied to the lifetime of a value. When the value goes out of scope, Rust automatically calls drop(), freeing the resource. No manual free(), no garbage collector. This makes Rust's memory management deterministic and leak-free in safe code.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-application", "raii"]
    },
    {
      "id": "CARD-058",
      "type": "cloze",
      "module": "M04",
      "lesson": "L10",
      "front": "The `match` expression in Rust must be {{exhaustive}} — if you don't handle all variants, the code {{won't compile}}.",
      "back": "This is a compile-time guarantee, not a runtime check. If you add a new variant to an enum later, every match on that enum becomes a compile error — forcing you to handle it. Use `_` as a catch-all only when you genuinely don't care about the remaining cases.",
      "source_ref": "S009",
      "confidence_required": "high",
      "tags": ["module-M04", "claim-C3", "type-cloze", "exhaustive-match"]
    },
    {
      "id": "CARD-059",
      "type": "definition",
      "module": "M08",
      "lesson": "L23",
      "front": "What is the Iterator trait's only required method?",
      "back": "`fn next(&mut self) -> Option<Self::Item>` — returns Some(item) while items remain, then None when exhausted. All other iterator methods (map, filter, sum, collect, etc.) are provided as default implementations built on top of next. Implementing only next() gives you access to the entire iterator ecosystem.",
      "source_ref": "S011",
      "confidence_required": "high",
      "tags": ["module-M08", "claim-C6", "type-definition", "iterator-trait"]
    },
    {
      "id": "CARD-060",
      "type": "cloze",
      "module": "M07",
      "lesson": "L19",
      "front": "Generic code in Rust is {{monomorphized}} at compile time — one generic function produces {{multiple concrete functions}}, one per type it's called with.",
      "back": "This is why Rust generics are zero-cost: no dynamic dispatch, no virtual method tables. The trade-off is longer compile times and larger binaries (each instantiation is compiled separately).",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-cloze", "monomorphization"]
    },
    {
      "id": "CARD-061",
      "type": "application",
      "module": "M06",
      "lesson": "L18",
      "front": "When is it worth defining a custom error enum instead of using Box<dyn Error>?",
      "back": "Use a custom error enum when: (1) callers need to match on specific error variants, (2) you are writing a library (type erasure in Box<dyn Error> removes caller flexibility), (3) you have multiple distinct failure modes that warrant different handling. For application main() or quick scripts, Box<dyn Error> is fine.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-application", "custom-error-when"]
    },
    {
      "id": "CARD-062",
      "type": "cloze",
      "module": "M05",
      "lesson": "L14",
      "front": "HashMap's {{get}} method returns `Option<{{&V}}>` — Some(&value) if found, None if absent.",
      "back": "Always returns a reference (borrow), not the owned value — you'd need remove() to take ownership. This ensures the HashMap retains ownership of its values while allowing read access.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-cloze", "hashmap-get"]
    },
    {
      "id": "CARD-063",
      "type": "distinction",
      "module": "M02",
      "lesson": "L04",
      "front": "Which types implement Copy in Rust, and which must be cloned?",
      "back": "Copy types (assignment copies, not moves): all integer types (i32, u64...), floating point (f64), bool, char, tuples/arrays of Copy types, references &T. Must be cloned: String, Vec<T>, HashMap, Box<T>, any heap-allocated type. The rule: if the type owns heap memory or a resource, it cannot be Copy.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M02", "claim-C2", "type-distinction", "copy-vs-clone"]
    },
    {
      "id": "CARD-064",
      "type": "cloze",
      "module": "M08",
      "lesson": "L24",
      "front": "`.collect::<Vec<_>>()` or `.collect::<HashMap<_, _>>()` — the type annotation is needed because collect can produce {{many different collection types}} and Rust needs a hint to resolve the {{ambiguity}}.",
      "back": "You can provide the type on the left: `let v: Vec<_> = iter.collect();` or inline: `iter.collect::<Vec<_>>()`. The `_` lets Rust infer the element type.",
      "source_ref": "S011",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-cloze", "collect-type"]
    },
    {
      "id": "CARD-065",
      "type": "application",
      "module": "M03",
      "lesson": "L08",
      "front": "When should you take &mut self vs self in a method?",
      "back": "&self: read-only access, can be called on owned values and references. &mut self: modify the instance, caller must have mutable binding. self: consume the instance (useful for builder pattern or conversion methods). Taking self is rare — it prevents the caller from using the value again. Default to &self; use &mut self when mutation is needed.",
      "source_ref": "S008",
      "confidence_required": "medium",
      "tags": ["module-M03", "claim-C1", "type-application", "self-choices"]
    },
    {
      "id": "CARD-066",
      "type": "cloze",
      "module": "M01",
      "lesson": "L03",
      "front": "In Rust, a function returns the value of its {{last expression}} (no semicolon). Adding a semicolon turns it into a {{statement}} that returns ().",
      "back": "```rust\nfn add(a: i32, b: i32) -> i32 {\n    a + b   // expression — returned\n}\n// vs\nfn nothing() -> () {\n    let _x = 5;  // statement — returns ()\n}\n```",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M01", "claim-C1", "type-cloze", "expression-return"]
    },
    {
      "id": "CARD-067",
      "type": "definition",
      "module": "M05",
      "lesson": "L15",
      "front": "What is the purpose of the format! macro vs String::from and +?",
      "back": "format!(\"{name} is {age}\") creates a new String by interpolating values — it doesn't take ownership of any argument. The + operator requires the left side to be an owned String and borrows the right side (&str) — messy for multi-string concatenation. Use format! for readable, ownership-safe string building; use push_str for appending in a loop.",
      "source_ref": "S001",
      "confidence_required": "low",
      "tags": ["module-M05", "claim-C1", "type-definition", "format-macro"]
    },
    {
      "id": "CARD-068",
      "type": "cloze",
      "module": "M07",
      "lesson": "L21",
      "front": "Lifetime elision rule 3: if one of the inputs is {{&self}} or {{&mut self}}, the lifetime of self is assigned to all output lifetimes — this is why most struct method return types need no lifetime annotation.",
      "back": "Example: `fn first_word(&self) -> &str` — the compiler infers the return &str borrows from self. No 'a annotation needed.",
      "source_ref": "S010",
      "confidence_required": "high",
      "tags": ["module-M07", "claim-C5", "type-cloze", "elision-rule-3"]
    },
    {
      "id": "CARD-069",
      "type": "application",
      "module": "M04",
      "lesson": "L12",
      "front": "Compare how Python and Rust handle the absence of a value in a dictionary lookup.",
      "back": "Python dict.get(key) returns None (untyped — the same None used everywhere, no compile-time guarantee). Rust HashMap::get(key) returns Option<&V> — the type system enforces that you handle both Some(&v) and None before using the value. A Python None isn't guaranteed to cause an error until it propagates; Rust's Option<T> is a compile-time guarantee the caller must address.",
      "source_ref": "S009",
      "confidence_required": "medium",
      "tags": ["module-M04", "claim-C3", "type-application", "option-vs-python-none"]
    },
    {
      "id": "CARD-070",
      "type": "cloze",
      "module": "M09",
      "lesson": "L25",
      "front": "JoinHandle::{{join}}() waits for the spawned thread to finish and returns `Result<T, Box<dyn Any>>` where T is the thread's {{return value}}.",
      "back": "If the thread panicked, join() returns Err. Calling join() on all spawned threads before exiting main prevents the program from killing running threads prematurely.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M09", "claim-C7", "type-cloze", "join-handle"]
    },
    {
      "id": "CARD-071",
      "type": "definition",
      "module": "M06",
      "lesson": "L17",
      "front": "What does the From trait have to do with the ? operator?",
      "back": "When ? encounters an Err(e), it calls From::from(e) to convert e into the function's declared error type. This allows ? to work across different error types automatically — as long as From<source_error_type> is implemented for the function's error type. For Box<dyn Error>, all error types that implement Error are automatically convertible.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-definition", "from-trait-question-mark"]
    },
    {
      "id": "CARD-072",
      "type": "cloze",
      "module": "M08",
      "lesson": "L22",
      "front": "A {{move}} closure takes {{ownership}} of its captured variables, making it useful for spawning threads or returning closures from functions.",
      "back": "Without move: closure captures by reference, requiring the referent to outlive the closure. With move: closure owns its captures, so it can outlive the original scope. The thread::spawn closure almost always requires move.",
      "source_ref": "S005",
      "confidence_required": "medium",
      "tags": ["module-M08", "claim-C6", "type-cloze", "move-closure"]
    },
    {
      "id": "CARD-073",
      "type": "application",
      "module": "M07",
      "lesson": "L20",
      "front": "A type implements Display. Does it automatically implement ToString?",
      "back": "Yes — via a blanket implementation in the Rust standard library: `impl<T: fmt::Display> ToString for T`. This is the standard way Rust provides method access based on trait bounds without requiring manual implementation for every type. The .to_string() method is available on any type implementing Display.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-application", "blanket-tostring"]
    },
    {
      "id": "CARD-074",
      "type": "cloze",
      "module": "M03",
      "lesson": "L07",
      "front": "A {{tuple struct}} like `struct Color(i32, i32, i32)` is accessed by {{index}}: `color.0`, `color.1`, `color.2`.",
      "back": "Tuple structs are useful when you want a distinct named type but don't need field names. Unlike plain tuples, `Color(255, 0, 0)` and `Point(255, 0, 0)` are different types even though they have the same fields.",
      "source_ref": "S008",
      "confidence_required": "low",
      "tags": ["module-M03", "claim-C1", "type-cloze", "tuple-struct"]
    },
    {
      "id": "CARD-075",
      "type": "distinction",
      "module": "M09",
      "lesson": "L27",
      "front": "Why is Rc<T> not thread-safe, and how does Arc<T> solve this?",
      "back": "Rc<T> uses non-atomic reference counting — incrementing and decrementing the count is not thread-safe (a data race on the count itself). Arc<T> uses atomic operations for reference counting, ensuring correctness when cloned across threads. The name Arc = Atomically Reference Counted. Arc has slightly higher overhead; only use it when threads are involved.",
      "source_ref": "S005",
      "confidence_required": "high",
      "tags": ["module-M09", "claim-C7", "type-distinction", "arc-vs-rc"]
    },
    {
      "id": "CARD-076",
      "type": "cloze",
      "module": "M06",
      "lesson": "L16",
      "front": "Calling `.unwrap()` on `Err(e)` causes a {{panic}} with the message: \"called Result::unwrap() on an Err value: {{e}}\".",
      "back": "unwrap() is useful during prototyping or in tests where you expect success. In production code, use expect(\"descriptive message\") for better error messages, or ? for proper propagation.",
      "source_ref": "S003",
      "confidence_required": "medium",
      "tags": ["module-M06", "claim-C5", "type-cloze", "unwrap-panic"]
    },
    {
      "id": "CARD-077",
      "type": "application",
      "module": "M05",
      "lesson": "L13",
      "front": "What is the risk of using v[i] vs v.get(i) when iterating with an index in Rust?",
      "back": "v[i] panics at runtime with 'index out of bounds' if i >= v.len(). v.get(i) returns None safely. When iterating with a range, ensure the range is bounded by v.len() — or better, avoid index-based iteration entirely and use iterators (for item in &v) which are always safe.",
      "source_ref": "S001",
      "confidence_required": "medium",
      "tags": ["module-M05", "claim-C1", "type-application", "vec-index-safety"]
    },
    {
      "id": "CARD-078",
      "type": "cloze",
      "module": "M02",
      "lesson": "L05",
      "front": "You can have {{many}} immutable references (&T) to a value OR {{one}} mutable reference (&mut T), but never {{both}} at the same time.",
      "back": "This is enforced at compile time by the borrow checker. The constraint enables the compiler to guarantee freedom from data races and aliasing bugs without any runtime cost.",
      "source_ref": "S002",
      "confidence_required": "high",
      "tags": ["module-M02", "claim-C2", "type-cloze", "borrow-rules"]
    },
    {
      "id": "CARD-079",
      "type": "definition",
      "module": "M08",
      "lesson": "L24",
      "front": "What does enumerate() return, and when is it useful?",
      "back": "enumerate() wraps an iterator to produce (index, value) pairs: `Iterator<Item = (usize, T)>`. Use it when you need both the element and its position: `for (i, item) in v.iter().enumerate()`. This replaces the classic `for i in 0..v.len()` index loop with a safe, idiomatic alternative.",
      "source_ref": "S011",
      "confidence_required": "low",
      "tags": ["module-M08", "claim-C6", "type-definition", "enumerate"]
    },
    {
      "id": "CARD-080",
      "type": "cloze",
      "module": "M09",
      "lesson": "L26",
      "front": "After `tx.send(value)`, the {{ownership}} of `value` is transferred to the {{receiver}}. This is why channels enable safe data transfer: no aliasing possible.",
      "back": "The Sender sends T (owned). The Receiver receives T (owned). The borrow checker ensures the sender has released its ownership before the receiver can use the value. This is the channel's safety guarantee.",
      "source_ref": "S005",
      "confidence_required": "high",
      "tags": ["module-M09", "claim-C7", "type-cloze", "channel-ownership"]
    },
    {
      "id": "CARD-081",
      "type": "application",
      "module": "M07",
      "lesson": "L19",
      "front": "Write the signature for a generic function that takes a slice of any type and returns the largest element, requiring the element type to be comparable.",
      "back": "`fn largest<T: PartialOrd>(list: &[T]) -> &T` — the PartialOrd trait bound enables the > operator. The function returns a reference to avoid moving elements out of the slice. This is one of the first examples in the Rust Book for generics with trait bounds.",
      "source_ref": "S004",
      "confidence_required": "medium",
      "tags": ["module-M07", "claim-C1", "type-application", "generic-function"]
    }
  ]
}
