{
  "id": "modcheck-M01",
  "module": "M01",
  "title": "Module Check: Modern Python Toolchain",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "State the distinct role of ruff, mypy, and pytest in the Python toolchain.",
      "answer": "ruff: fast linter + formatter (style, imports, common errors — no type inference). mypy: static type checker (verifies type annotations, catches type mismatches). pytest: test runner (executes and reports test results). They are complementary, not alternatives."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "Why is pyproject.toml the preferred location for tool configuration in modern Python projects?",
      "answer": "pyproject.toml serves two roles: (1) build system configuration (PEP 517/518) replacing setup.py. (2) Unified tool config replacing separate setup.cfg, .flake8, mypy.ini, pytest.ini. A single file reduces configuration sprawl and makes the project easier to onboard."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "What is the difference between automated checks (ruff/mypy) and human code review, and which bugs does each catch?",
      "answer": "Automated checks catch deterministic rule violations: undefined names, type mismatches, style inconsistencies, unused imports — fast and exhaustive within their rule set. Human review catches design flaws, unclear intent, missing business logic, inappropriate abstractions — things that require context and judgment."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "Which command checks types in a Python file using mypy?",
      "options": {
        "A": "mypy --check src/",
        "B": "mypy src/",
        "C": "python -m mypy --strict src/",
        "D": "mypy --run src/"
      },
      "correct": "B",
      "explanation": "The basic invocation is `mypy src/` or `mypy file.py`. `python -m mypy` also works. There is no `--check` flag. `--strict` enables stricter checks but is optional. `--run` is not a mypy flag."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "What is the consolidation argument for using ruff instead of separate flake8 + isort + black?",
      "options": {
        "A": "ruff supports more rule sets than flake8",
        "B": "ruff is 10–100x faster and replaces all three tools in one binary with unified config",
        "C": "ruff enforces type annotations that flake8 ignores",
        "D": "black and isort conflict on import formatting; ruff resolves this"
      },
      "correct": "B",
      "explanation": "ruff is written in Rust and runs 10–100x faster than the Python tools. It implements flake8 rules, isort-compatible import sorting, and black-compatible formatting — configured in a single `[tool.ruff]` section in pyproject.toml. Fewer tools, less CI config, faster feedback."
    }
  ]
}