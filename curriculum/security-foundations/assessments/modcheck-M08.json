{
  "id": "modcheck-M08",
  "module": "M08",
  "title": "Module Check: Credential Attacks and Kerberos",
  "generated": "2026-04-14",
  "questions": [
    {
      "id": "MC01",
      "type": "open",
      "question": "Why is the NTLM hash functionally equivalent to a plaintext password for authentication?",
      "answer": "NTLM authentication uses challenge-response: the client proves identity by hashing a server challenge with the NT hash. An attacker with the NT hash can compute the correct response without knowing the original password. They never need to crack it — they authenticate directly. This is the pass-the-hash (PtH) attack."
    },
    {
      "id": "MC02",
      "type": "open",
      "question": "What Event 4624 signature distinguishes a pass-the-hash authentication from a normal network login?",
      "answer": "PtH produces a Type 3 (Network) logon with AuthenticationPackageName = NTLM and LogonProcessName = NtLmSsp. The connecting IP is the attacker's machine. A normal Kerberos login uses AuthenticationPackageName = Kerberos. Alert on NTLM Type 3 logons from non-standard hosts, especially to high-value targets."
    },
    {
      "id": "MC03",
      "type": "open",
      "question": "Why does Kerberoasting target service accounts specifically, and what makes RC4 the preferred encryption for the attack?",
      "answer": "Service accounts have SPNs (Service Principal Names) — any domain user can request a Kerberos service ticket encrypted with the service account's password hash. The attacker takes the ticket offline and cracks it. RC4-HMAC (type 23) is preferred because it is faster to crack than AES-256 by several orders of magnitude. Forcing RC4 downgrade is part of the attack."
    },
    {
      "id": "MC04",
      "type": "mcq",
      "question": "How does setting PowerShell execution policy to \"Restricted\" defend against script-based attacks?",
      "options": {
        "A": "It prevents all PowerShell execution, including LOL (living-off-the-land) attacks",
        "B": "It is a meaningful security boundary that blocks unsigned scripts",
        "C": "It is bypassable with `-ExecutionPolicy Bypass` or `powershell -enc` and provides no real security",
        "D": "It prevents script injection only from remote sources"
      },
      "correct": "C",
      "explanation": "Execution policy is not a security boundary — it is a convenience feature. Any local admin or the current user can bypass it with `powershell.exe -ExecutionPolicy Bypass script.ps1` or by encoding the script. AMSI (Antimalware Scan Interface) and AppLocker are the actual controls for restricting PowerShell script execution."
    },
    {
      "id": "MC05",
      "type": "mcq",
      "question": "What is the \"paired-detection principle\" for credential attacks?",
      "options": {
        "A": "Always deploy two EDR agents to ensure coverage",
        "B": "Pair each attack technique with two independent detection methods from different data sources",
        "C": "Correlate authentication events from both the source and destination host",
        "D": "Alert only when two separate threat intelligence feeds both flag a host"
      },
      "correct": "B",
      "explanation": "The paired-detection principle: for each technique, implement at least two detections from independent data sources (e.g., Event 4624 and Sysmon 3 for lateral movement). If one detection is evaded or the log source is disabled, the second provides coverage. Single-source detection has a single point of failure."
    }
  ]
}