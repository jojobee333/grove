{
  "quiz_id": "quiz-M03",
  "module_id": "M03",
  "title": "Module 3 Quiz — Collections & Iteration",
  "passing_score": 70,
  "questions": [
    {
      "id": "q01",
      "type": "mcq",
      "prompt": "Which iterator should you use to loop over values in an array?",
      "options": ["for...in", "for...of", "forEach only", "while"],
      "correct": 1,
      "explanation": "for...of iterates over values. for...in iterates over keys as strings and should not be used on arrays."
    },
    {
      "id": "q02",
      "type": "mcq",
      "prompt": "What does [...arr] produce?",
      "options": [
        "A deep clone",
        "A shallow copy — shared object references",
        "A frozen array",
        "A new array from arr's prototype chain"
      ],
      "correct": 1,
      "explanation": "Spread creates a shallow copy. Primitive values are copied; object references inside are shared."
    },
    {
      "id": "q03",
      "type": "mcq",
      "prompt": "Which method returns a single accumulated value from an array?",
      "options": ["map", "filter", "reduce", "find"],
      "correct": 2,
      "explanation": "reduce() accumulates all elements into a single output value using a callback and initial value."
    },
    {
      "id": "q04",
      "type": "mcq",
      "prompt": "const obj = { a: 1 }; const copy = { ...obj, b: 2, a: 99 }; What is copy.a?",
      "options": ["1", "99", "undefined", "Error"],
      "correct": 1,
      "explanation": "Later properties win in a spread. a: 99 comes after ...obj's a: 1, so it overrides to 99."
    },
    {
      "id": "q05",
      "type": "short_answer",
      "prompt": "Write a one-liner to get total price from all active items in an array of { price, active } objects.",
      "answer_key": "items.filter(i => i.active).reduce((sum, i) => sum + i.price, 0)"
    },
    {
      "id": "q06",
      "type": "mcq",
      "prompt": "How do you make a proper deep copy of an object with nested arrays and dates?",
      "options": [
        "{ ...obj }",
        "Object.assign({}, obj)",
        "JSON.parse(JSON.stringify(obj))",
        "structuredClone(obj)"
      ],
      "correct": 3,
      "explanation": "structuredClone() is the modern built-in for deep cloning. JSON.parse(JSON.stringify()) loses Date objects, undefined values, and functions."
    }
  ]
}
